# Frontend

This folder will contain the frontend code for OverXchange.

- React app
- UI components
- Static assets
start
  python -m http.server 8000 --directory frontend
Start here to build the client-side of the application. 